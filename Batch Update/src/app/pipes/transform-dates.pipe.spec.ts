import { TransformDatesPipe } from './transform-dates.pipe';

describe('TransformDatesPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformDatesPipe();
    expect(pipe).toBeTruthy();
  });
});
